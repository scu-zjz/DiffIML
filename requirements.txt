# Core deep-learning stack
# (Install torch / torchvision separately matching your CUDA, e.g.:
#   pip install torch==2.1.0 torchvision==0.16.0 --index-url https://download.pytorch.org/whl/cu118
# then run: pip install -r requirements.txt)
numpy<2
scipy>=1.10
Pillow>=9.0
opencv-python>=4.7
scikit-image>=0.20
scikit-learn>=1.2
matplotlib>=3.6
tqdm>=4.65
pyyaml>=6.0

# HuggingFace stack
diffusers>=0.25.0
transformers>=4.36.0
accelerate>=0.25.0
huggingface_hub>=0.20.0
safetensors>=0.4.0

# Vision / data augmentation
albumentations>=1.3.0
timm>=0.9.0
torchinfo>=1.8.0
einops>=0.7.0

# Logging & metrics
tensorboard>=2.14
wandb>=0.16.0   # optional
prettytable>=3.8

# Utilities
ftfy
regex
filelock
packaging
